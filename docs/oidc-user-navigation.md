# OIDC user navigation

Tracks issue #35: show only the authenticated user's name in the sidebar, make the user area link to `/oidc`, use the shared sidebar on the OIDC page, and expose site-admin user management from the sidebar.
